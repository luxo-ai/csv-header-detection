{
  "name": "csv_header_detection",
  "version": "1.0.0",
  "description": "Detect CSV Headers",
  "main": "index.js",
  "author": "@luxo-ai",
  "license": "MIT",
  "private": false,
  "scripts": {
 "build": "rimraf ./dist && tsc",
 "start": "yarn build && node ./dist/index.js" 
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  }
}
